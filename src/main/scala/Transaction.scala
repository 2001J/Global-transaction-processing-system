case class Transaction(
                        id: String,
                        amount: Double,
                        currency: String,
                        timestamp: Long,
                        ipAddress: String
                      )